---
import BaseLayout from '../layouts/BaseLayout.astro';
import research from '../data/research-page.json';
---
<BaseLayout title="Research" description="Three core research themes in translational immunology.">
  <section class="section">
    <h1 class="text-4xl font-semibold tracking-tight">Research</h1>
    <p class="mt-3 max-w-3xl text-slate-700">{research.intro}</p>
    <div class="mt-8 space-y-5">
      {research.themes.map((theme) => (
        <article class="card">
          <h2 class="text-2xl font-semibold">{theme.title}</h2>
          <p class="mt-3 text-slate-700">{theme.text}</p>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
